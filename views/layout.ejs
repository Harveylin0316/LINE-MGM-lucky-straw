<!DOCTYPE html>
<html lang="zh-Hant">
  <head>
    <meta charset="UTF-8" />
    <title>春日饗里 LINE MGM 刮刮樂 後台</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <header class="topbar">
      <div class="container">
        <div class="brand">春日饗里 LINE MGM 刮刮樂 後台</div>
        <nav>
          <% if (user) { %>
          <span class="welcome">嗨，<%= user %></span>
          <% if (isAdmin) { %>
          <a href="/admin/prizes">獎品設定</a>
          <a href="/admin/campaign">活動時間</a>
          <a href="/admin/reports">活動成效報表</a>
          <a href="/admin/line/webhooks">LINE Webhook 診斷</a>
          <% } %>
          <form action="/logout" method="post" class="inline-form">
            <button type="submit">登出</button>
          </form>
          <% } else { %>
          <span class="muted">管理入口為內部專用</span>
          <% } %>
        </nav>
      </div>
    </header>
    <main class="container">
      <%- body %>
    </main>
  </body>
</html>

