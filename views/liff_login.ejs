<%-
  include('layout_liff', {
    body: `
    <section class="liff-card">
      <h1>LIFF 抽獎入口</h1>
      <p class="muted">你尚未登入。先使用現有帳號登入後，即可進入 LIFF 抽獎頁。</p>
      <div class="actions">
        <a class="btn primary" href="/login?next=${encodeURIComponent(nextPath || '/liff/lottery')}">前往登入</a>
        <a class="btn" href="/register">註冊新帳號</a>
      </div>
    </section>
  `
  })
%>
